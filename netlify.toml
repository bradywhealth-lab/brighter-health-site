[build]
  # Prepare a Gatsby-compatible publish folder for this static site
  command = "bash -c 'set -euo pipefail; rm -rf public; mkdir -p public; cp index.html public/; cp landing.html public/; if [ -d assets ]; then cp -r assets public/; fi'"
  publish = "public"
