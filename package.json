{
  "name": "healthwithbrady",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site for an independent health advisor.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "rm -rf public && mkdir -p public/assets && cp index.html public/index.html && cp landing.html public/landing.html && cp -R assets/. public/assets/"
  }
}
